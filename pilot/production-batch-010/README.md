# Production batch 010

Batch 010 adds ten source-order records around the third-section fillings and
pastries: `vera-r0073`–`vera-r0078` and `vera-r0080`–`vera-r0083`, from PDF
pages 50–54. Existing `vera-r0079` Vanilla sauce was read back only and was not
overwritten.

## Trust result

Nine new records are `source_checked`: `vera-r0073`–`vera-r0076`,
`vera-r0078`, and `vera-r0080`–`vera-r0083`. A later owner source decision
classified `vera-r0077`’s outlined-arrow ornament as non-text decoration, so
all ten new records are `source_checked`. Czech doughnuts owns its PDF 53–54
continuation. Candidate snapshots were preserved before the separate fresh
local AI source audit, which is not human verification.

Evidence contains five full-page source images, eleven recipe-overview images,
and 54 cited-region crops with decoded-pixel hashes and dimensions. The local
review page is `review/production-batch-010/index.html`.

Marker used the local-only wave-005 run recorded in Batch 009; its JSON and
metadata hashes are repeated there. No remote LLM, page upload, or remote
extraction service was used.
