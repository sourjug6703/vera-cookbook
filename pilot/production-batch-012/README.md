# Production batch 012

Batch 012 adds ten records, `vera-r0094` through `vera-r0103`, across PDF pages
62–70. `vera-r0101` is one composite fritter-family record spanning PDF pages
67–68; its shared batter, fruit variants, and accompaniments do not become
separate recipe IDs.

## Trust result

All ten new records are `source_checked` after a separate fresh direct local AI
visual source audit, not human verification. Country pancakes retains its
main, topping, and optional ingredient ownership from the shared visual block.
All original `machine_candidate` snapshots were retained before promotion.

Evidence contains nine full-page source images, eleven recipe-overview images,
and 85 cited-region crops with decoded-pixel hashes and dimensions. The local
review page is `review/production-batch-012/index.html`.

Marker used the local-only wave-005 run recorded in Batch 009; no remote LLM,
page upload, or remote extraction service was used.
